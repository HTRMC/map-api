#include "MapRenderer.h"
#include <iostream>

void MapRenderer::renderMap(double lat, double lon, const std::string& label) {
    std::cout << "Rendering map with marker at (" << lat << ", " << lon << ") labeled '" << label << "'" << std::endl;
    // Hier zou je een echte kaart-rendering bibliotheek gebruiken
}